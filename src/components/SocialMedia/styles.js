import styled from 'styled-components';

export const Container = styled.div`
  width: 150px;
  display: flex;
  justify-content: space-between;
  
  svg {
    width: 18px;
    height: 18px;
    color: var(--black);
    cursor: pointer;
  }
`;